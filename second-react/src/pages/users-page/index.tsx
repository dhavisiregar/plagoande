import UserList from "../../components/UserList";

const Users = () => {
  return (
    <main className="w-screen h-full flex gap-y-5 justify-center items-center bg-white text-black">
      <UserList />
    </main>
  );
};

export default Users;
